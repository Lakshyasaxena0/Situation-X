[build]
  command = "pnpm install --no-frozen-lockfile && cd frontend && pnpm run build"
  publish = "frontend/.next"

[build.environment]
  NEXT_PUBLIC_API_URL = "https://your-backend-url.onrender.com/api"
  NODE_VERSION = "22"

[[plugins]]
  package = "@netlify/plugin-nextjs"
