[build]
  command = "pnpm install --no-frozen-lockfile && cd frontend && pnpm run build"
  publish = "frontend/.next"

[build.environment]
  NEXT_PUBLIC_API_URL = "https://situation-x.onrender.com/api"
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
